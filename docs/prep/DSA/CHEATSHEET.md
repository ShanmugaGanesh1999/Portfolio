# 🎯 DSA Problem-Solving Cheatsheet

## 📊 Decision Flowchart

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        🔍 READ THE PROBLEM                                  │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                     What is the INPUT type?                                 │
└─────────────────────────────────────────────────────────────────────────────┘
        │           │           │           │           │
        ▼           ▼           ▼           ▼           ▼
    ┌───────┐  ┌───────┐  ┌───────┐  ┌───────┐  ┌───────┐
    │ Array │  │String │  │ Tree  │  │ Graph │  │Linked │
    │       │  │       │  │       │  │       │  │ List  │
    └───┬───┘  └───┬───┘  └───┬───┘  └───┬───┘  └───┬───┘
        │          │          │          │          │
        ▼          ▼          ▼          ▼          ▼
   See Array   See String    DFS        DFS     Two Pointers
    Section     Section      BFS        BFS     Fast & Slow
```

---

## 🗺️ Pattern Selection Map

### 📌 IF Input Array is SORTED
```
┌────────────────────────┐
│   SORTED ARRAY?        │
└──────────┬─────────────┘
           │
     ┌─────┴─────┐
     ▼           ▼
┌─────────┐ ┌──────────────┐
│ Binary  │ │ Two Pointers │
│ Search  │ │              │
└─────────┘ └──────────────┘
     │              │
     ▼              ▼
• Find target   • Two Sum II
• Search range  • 3Sum
• Find min/max  • Container with Water
• Insert pos    • Remove duplicates
```

---

### 📌 IF Asked for All Permutations/Subsets
```
┌────────────────────────────────┐
│  ALL PERMUTATIONS / SUBSETS?   │
└──────────────┬─────────────────┘
               │
               ▼
        ┌──────────────┐
        │ BACKTRACKING │
        └──────────────┘
               │
               ▼
    • Permutations
    • Combinations  
    • Subsets
    • N-Queens
    • Sudoku Solver
    • Word Search
```

---

### 📌 IF Given a Tree
```
┌─────────────────┐
│   TREE INPUT?   │
└────────┬────────┘
         │
    ┌────┴────┐
    ▼         ▼
┌───────┐ ┌───────┐
│  DFS  │ │  BFS  │
└───────┘ └───────┘
    │         │
    ▼         ▼
• Max depth    • Level order
• Path sum     • Zigzag traversal
• Validate BST • Right side view
• LCA          • Min depth
• Serialize    • Connect next ptr
```

---

### 📌 IF Given a Graph
```
┌─────────────────┐
│  GRAPH INPUT?   │
└────────┬────────┘
         │
    ┌────┴────┐
    ▼         ▼
┌───────┐ ┌───────┐
│  DFS  │ │  BFS  │
└───────┘ └───────┘
    │         │
    ▼         ▼
• Cycle detect  • Shortest path
• Connected     • Word ladder
  components    • Rotting oranges
• Topological   • 01 Matrix
  sort          • Bipartite
• Clone graph   
```

---

### 📌 IF Given a Linked List
```
┌─────────────────────┐
│  LINKED LIST INPUT? │
└──────────┬──────────┘
           │
           ▼
    ┌──────────────┐
    │ TWO POINTERS │
    └──────────────┘
           │
      ┌────┴────┐
      ▼         ▼
┌──────────┐ ┌──────────┐
│ Fast &   │ │ Gap      │
│ Slow     │ │ Pointers │
└──────────┘ └──────────┘
      │            │
      ▼            ▼
• Find middle   • Nth from end
• Detect cycle  • Intersection
• Find cycle    • Palindrome
  start           check
```

---

### 📌 IF Recursion is Banned
```
┌─────────────────────┐
│  RECURSION BANNED?  │
└──────────┬──────────┘
           │
           ▼
      ┌─────────┐
      │  STACK  │
      └─────────┘
           │
           ▼
    • Iterative DFS
    • Tree traversals
    • Expression eval
    • Parentheses matching
```

---

### 📌 IF Must Solve In-Place
```
┌─────────────────────┐
│   SOLVE IN-PLACE?   │
└──────────┬──────────┘
           │
      ┌────┴────┐
      ▼         ▼
┌───────────┐ ┌────────────────┐
│   SWAP    │ │ STORE MULTIPLE │
│  VALUES   │ │ VALUES IN PTR  │
└───────────┘ └────────────────┘
      │               │
      ▼               ▼
• Reverse array   • Use sign bit
• Sort colors     • Use index as
• Rotate array      hash
• Move zeros      • Encode two vals
```

---

### 📌 IF Asked for Max/Min Subarray/Subset
```
┌─────────────────────────────────┐
│  MAX/MIN SUBARRAY or SUBSET?   │
└───────────────┬─────────────────┘
                │
        ┌───────┴───────┐
        ▼               ▼
┌────────────────┐ ┌────────────────┐
│    DYNAMIC     │ │    SLIDING     │
│  PROGRAMMING   │ │    WINDOW      │
└────────────────┘ └────────────────┘
        │                   │
        ▼                   ▼
• House robber        • Max sum (k)
• Coin change         • Longest unique
• Longest subseq      • Min window
• Partition equal     • Fruit baskets
```

---

### 📌 IF Asked for Top/Least K Items
```
┌─────────────────────────┐
│   TOP/LEAST K ITEMS?    │
└───────────┬─────────────┘
            │
      ┌─────┴─────┐
      ▼           ▼
┌──────────┐ ┌─────────────┐
│   HEAP   │ │ QUICKSELECT │
└──────────┘ └─────────────┘
      │             │
      ▼             ▼
• Kth largest    • Kth element
• Top K freq     • Median
• K closest      • K smallest
• Merge K lists
```

---

### 📌 IF Asked for Common Strings
```
┌─────────────────────────┐
│    COMMON STRINGS?      │
└───────────┬─────────────┘
            │
      ┌─────┴─────┐
      ▼           ▼
┌──────────┐ ┌──────────┐
│ HASH MAP │ │   TRIE   │
└──────────┘ └──────────┘
      │            │
      ▼            ▼
• Group anagrams  • Autocomplete
• Word pattern    • Word search II
• Isomorphic      • Prefix match
```

---

### 📌 DEFAULT (When Nothing Else Fits)
```
┌─────────────────────────────────┐
│       NOTHING ELSE FITS?        │
└───────────────┬─────────────────┘
                │
        ┌───────┴───────┐
        ▼               ▼
┌────────────────┐ ┌────────────────┐
│   MAP / SET    │ │  SORT INPUT    │
│                │ │                │
│  O(1) time     │ │  O(n log n)    │
│  O(n) space    │ │  O(1) space    │
└────────────────┘ └────────────────┘
```

---

## ⚡ Quick Reference Table

| Condition | Pattern | Time | Space |
|-----------|---------|------|-------|
| Sorted array | Binary Search | O(log n) | O(1) |
| Sorted array | Two Pointers | O(n) | O(1) |
| All permutations/subsets | Backtracking | O(n!) / O(2ⁿ) | O(n) |
| Tree traversal | DFS / BFS | O(n) | O(h) / O(w) |
| Graph traversal | DFS / BFS | O(V+E) | O(V) |
| Linked list | Two Pointers | O(n) | O(1) |
| No recursion | Stack | O(n) | O(n) |
| In-place | Swap / Encode | O(n) | O(1) |
| Max/min subarray | DP / Sliding Window | O(n) | O(n) / O(1) |
| Top K items | Heap | O(n log k) | O(k) |
| Top K items | QuickSelect | O(n) avg | O(1) |
| Common strings | HashMap / Trie | O(n*m) | O(n*m) |
| Default | HashMap/Set | O(n) | O(n) |
| Default | Sort | O(n log n) | O(1) |

---

## 🧠 Memory Tricks

```
"SORTED? → SEARCH or SQUEEZE"     (Binary Search / Two Pointers)
"ALL COMBOS? → BACKTRACK"         (Backtracking)
"TREE/GRAPH? → EXPLORE"           (DFS/BFS)
"LINKED? → RACE"                  (Fast & Slow)
"NO RECURSE? → STACK IT"          (Stack)
"IN-PLACE? → SWAP or ENCODE"      (Swap / Bit manipulation)
"OPTIMIZE? → DP or SLIDE"         (DP / Sliding Window)
"TOP K? → HEAP IT"                (Heap / QuickSelect)
"STRINGS? → MAP or TRIE"          (HashMap / Trie)
"STUCK? → HASH or SORT"           (HashMap / Sort)
```

---

## 🎯 Interview Checklist

```
□ Read problem carefully
□ Identify input type (array, tree, graph, etc.)
□ Check if sorted
□ Identify what's being asked (max, min, count, all, etc.)
□ Match to pattern above
□ Consider constraints (in-place, no recursion, etc.)
□ Code solution
□ Test with examples
□ Analyze complexity
```

---

**Pro Tip**: When stuck, ask yourself:
1. Can I sort it? → O(n log n) might be acceptable
2. Can I use extra space? → HashMap for O(1) lookup
3. Have I seen something similar? → Pattern matching!

---

*Good luck with your interviews! 🚀*
