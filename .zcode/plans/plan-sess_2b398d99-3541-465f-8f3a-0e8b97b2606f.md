# Remove Codex + Rebuild Claude Code on the provided HTML base

**Source of truth:** `/Users/sg/Downloads/claude-code-terminal.html` — its structure, palette, typography, and behaviors become the base; portfolio functionality works on top.

## A. Remove Codex entirely

- Delete `src/shell/codex/` (4 files)
- Workspace state: validate `vscode | claude` only; a stale saved `codex` preference falls back to VS Code
- `ModeSwitcher` back to two segments (`{ } VS Code | ✳ Claude Code`)
- Remove the `codex` shell command + help line, the Codex CSS tokens/shimmer, the Codex diagram palettes, and the Inter/IBM Plex font link (no longer used)
- `App.jsx` drops the CodexShell lazy branch

## B. Rebuild the Claude Code shell to the reference design

**Fidelity approach:** port the reference CSS nearly verbatim into `index.css` (scoped under `[data-shell="claude"]`), then build React components that emit the same classes — guaranteed pixel-fidelity instead of re-deriving with utilities.

**The window** — the biggest visual change: no longer a full-bleed app. A **floating terminal window** (max-width 1320px, rounded 10px, border + shadow) centered on a desktop backdrop (`#111110` dark / `#e9e7e3` light), exactly like the file:
- **Titlebar** (43px, `#292929`): traffic lights left (close → "See you next time" end screen with mascot + restart; minimize; maximize), centered `~/portfolio — claude` title with folder icon, compact VS Code switcher in the right meta slot
- **Viewport scrollback** with the reference's exact entry grammar:
  - **Shell context**: `~/portfolio git:(main)` (blue path, green branch) + `❯ claude` launch line
  - **Welcome panel**: two-column (45/55) with the border-broken "Claude Code v3.0" heading in terracotta, pixel mascot `▐▛███▜▌`, "Welcome back!" greeting, model + directory buttons, "Tips for getting started" with clickable inline commands, and **Recent activity** (live — shows your actual last command + "just now")
  - **✻ Tip line**, then the portfolio document (hero, stats, tech tree, career, projects, contact — restyled to reference tokens, anchors kept for `/about` etc.)
  - **Entries**: `❯` user turns in surface boxes, `●` assistant turns, `✻` system lines, **collapsible tool entries** (green `●` + bold name + `(path)` + "click to expand" hint, `⎿` output stems) — real `<details>` elements
  - **Thinking row** at transcript end: `·✢✳✶✻✽` spinner cycling at 140ms, verb + "esc to interrupt"
- **Composer**: `❯` symbol + **borderless textarea between two hairlines** (no box!), blinking **block cursor** when empty, send button that becomes a stop square while busy; 16px input on mobile
- **Statusline**: `? for shortcuts` + **permission mode button** (`default mode` / `⏵⏵ accept edits on` green / `⏸ plan mode on` purple — Shift+Tab cycles) | right: green dot + session state + running agents count
- **Slash menu**: reference styling (130px accent-name column + muted description, footer `↑ ↓ navigate · enter select · tab complete · esc dismiss`)
- **Modal panels** for `/model` (real chatService models), `/theme`, and `/config` — name, working-directory label, **font size (12–20px)**, theme; persisted to localStorage, applied as the terminal's font-size
- **Full keyboard set** from the file: Enter/Shift+Enter, ↑↓ history at first/last line, Tab complete, Shift+Tab mode cycle, Esc interrupt/dismiss, `?` help, Ctrl+C stop/clear, Ctrl+L jump-to-end; auto-scroll pauses when scrolled up

**Palette** (replaces current claude tokens): dark `#1c1c1c` terminal / `#232323` surface / `#e8e6e1` text / `#a39e98` muted / `#d58c6b` accent / `#44413d` border / `#9cba84` green; light `#faf9f6` / `#f0eee9` / `#302d29` / `#6c655e` / `#a85d3c` / `#d2cbc2` / `#466738`. **Typography: system mono only** (SFMono/Consolas/Menlo), 15px base — all Google fonts dropped from this shell.

**Kept functional guts** (hardened, restyled): natural-language → RAG streaming with thinking verbs, `!` shell passthrough (shared executor), `/open` project deep-dives as tool-entry + embedded file views, researcher/explorer agents and `/tour` (shown as tool entries + statusline count), permission dialogs restyled to the panel aesthetic.

## C. Verification

Build + lint; browser QA including a **side-by-side screenshot comparison against your HTML file itself** (opened via file://) in dark and light; full flow pass (welcome, slash menu, mode cycling, panels, config font-size, agents, streaming, window controls, end/restart screen); 2-shell × viewport × theme matrix; mobile 375px checks. Everything still uncommitted until you say ship.